# 红利股票跟踪系统 · 详细安装使用说明（8.17）

一套免费、可每天自动更新的 A 股红利股票跟踪工具：网页上查看股票列表（股息率TTM、市盈率TTM、市净率、总市值、ROE 等），点击个股看 K线图（均线/BOLL/DMI），并按你设定的阈值把“跌破/涨破”提醒推送到微信。

---

## 一、系统由什么组成

| 部分 | 说明 |
|---|---|
| 网页前端 | index.html / app.js / style.css，手机电脑浏览器直接打开；个股详情内含「公司信息」模块；首页含排名区块、标签筛选页（国有企业等）、浏览器返回键 |
| 数据抓取 | pipeline/ 下的 Python 程序（纯标准库，无需安装任何第三方包） |
| 股票池 | data/meta/watchlist.json（992只）+ fund_holdings.json（红利基金近5年持仓明细） |
| 每日数据 | data/snapshot.json（最新指标）、data/kline/（K线，自动生成）、data/history/（指标历史） |
| 公司基本面 | data/company/xxx.json（控股股东/分红/财务绩效/高管增减持/简介，带30天缓存、每日增量刷新） |
| 微信提醒 | pipeline/alerts.py + data/meta/alerts.json（阈值规则） |
| 云端定时 | .github/workflows/daily.yml（每天15:15自动更新+部署）、build-watchlist.yml（重建股票池）、build-company.yml（全量重建公司基本面） |

## 二、目录结构

```
红利股票跟踪8.16/
├── index.html / app.js / style.css   网页
├── config.json                        可调参数（股票池来源、历史保留天数等）
├── README.md                          简要说明
├── 安装使用说明8.17.md                本文件
├── pipeline/                          抓数与提醒程序
│   ├── build_watchlist.py             构建/更新股票池
│   ├── update_daily.py                每日数据更新（含公司基本面增量抓取）
│   ├── build_company.py               全量重建公司基本面（首次初始化/手动）
│   ├── company.py                     公司基本面抓取与清洗（控股股东/分红/财务/高管/简介）
│   ├── alerts.py                      股息率阈值提醒
│   ├── sources_funds.py               红利基金列表与持仓抓取
│   ├── sources_market.py              估值/ROE/分红/K线/板块/成分股抓取
│   └── net.py                         HTTP 请求封装（自动重试、防限流）
├── data/
│   ├── meta/watchlist.json            最终股票池（程序生成的，别手改）
│   ├── meta/fund_holdings.json        红利基金近5年持仓记录
│   ├── meta/alerts.json               你的提醒规则（可手改）
│   ├── meta/boards.json               板块市值排名
│   ├── meta/latest.json               最新更新信息
│   ├── snapshot.json                  当日指标快照
│   ├── history/xxx.json               每只股票的指标历史
│   ├── company/xxx.json               每只股票的公司基本面（带30天缓存，进仓库）
│   └── kline/xxx.json                 K线数据（自动生成，不进备份）
└── .github/workflows/                 云端定时任务
```

## 三、环境要求

- **Python 3.10 或更高**（推荐 3.12）；本程序只用标准库，**不需要 pip install 任何东西**；
- Windows / macOS / Linux 均可；
- 需要能访问互联网（抓取东方财富、腾讯、新浪、天天基金等公开数据）。

## 四、本地安装（两步）

### 1. 安装 Python

- Windows：到 [python.org/downloads](https://www.python.org/downloads/) 下载安装，安装时**勾选 “Add Python to PATH”**；
- 安装后打开终端（PowerShell），输入 `python --version` 能显示版本号即可。

### 2. 解压本备份

把压缩包解压到任意目录（路径最好别带空格，例如 `D:\hongli`）。进入目录后，所有命令都在这个目录下执行。

## 五、本地使用

### 每日更新数据（手动）

```
python pipeline/update_daily.py
```

会抓取当日估值/ROE/分红/K线，并从东方财富 F10 抓取每只股票的公司基本面（控股股东、分红、财务绩效、高管增减持、简介），生成网页数据。公司基本面带 30 天缓存，每天只增量刷新过期项，几分钟完成。

公司基本面首次需全量抓取（992 只，约 5~10 分钟），可手动跑一次全量构建：

```
python pipeline/build_company.py          # 全量抓取全部股票
python pipeline/build_company.py --limit 10   # 仅前10只（测试）
python pipeline/build_company.py --force      # 忽略缓存全部重抓
```

### 本地预览网页

```
python -m http.server 8000
```

浏览器打开 `http://localhost:8000` 即可。手机想看：确保手机和电脑在**同一个 WiFi**，浏览器打开 `http://电脑的IP:8000`（电脑IP可在终端输入 `ipconfig` 查看 IPv4 地址）。

### 本地定时自动更新（可选，Windows）

1. 按 `Win+R` 输入 `taskschd.msc` 回车，打开“任务计划程序”；
2. 右侧“创建基本任务”→ 名称随意（如“红利数据更新”）→ 触发器选“每天”，时间 15:15；
3. 操作选“启动程序”：
   - 程序：你的 python.exe 完整路径（如 `C:\Users\你的用户名\AppData\Local\Programs\Python\Python312\python.exe`，可用 `where python` 查到）；
   - 添加参数：`pipeline/update_daily.py`；
   - 起始于：本项目的完整路径（如 `D:\hongli`）；
4. 完成。以后每天 15:15 自动更新（周末/节假日会自动跳过）。

> 提示：如果电脑不常开，推荐直接用下面的“云端部署”，让 GitHub 帮你跑，不用管电脑。

## 六、云端部署（推荐，手机随时看）

全部免费，仓库公开（里面只有公开行情数据，无个人敏感信息）。

### 第 1 步：注册 GitHub 并建仓库

1. 打开 [github.com](https://github.com) 注册登录；
2. 右上角 **+ → New repository**，仓库名随意（如 `dividend-tracker`），选 **Public**，**不要勾选**任何初始化文件，Create。

### 第 2 步：把本项目推上去

在本项目目录执行（把 `用户名` 换成你的）：

```
git remote add origin https://github.com/用户名/dividend-tracker.git
git push -u origin main
```

第一次推送会要账号密码：
- 用户名：你的 GitHub 用户名；
- 密码：**不是登录密码**，是 Personal Access Token。生成方法：GitHub 头像 → Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token (classic) → 勾选 **repo** 和 **workflow** 两项 → Generate → 复制 `ghp_` 开头的字符串（只显示一次）。

> 常见错误：提示“refusing to allow a Personal Access Token ... without workflow scope” = 令牌没勾 workflow，去令牌设置里补勾即可。

### 第 3 步：开启网页（GitHub Pages）

1. 仓库 → **Settings → Pages** → Source 选 **GitHub Actions** → Save；
2. 仓库 → **Actions** → 左侧选 **daily-update** → **Run workflow**，等几分钟跑完（出现绿色对勾）；
3. 手机/电脑打开：`https://用户名.github.io/dividend-tracker/`

### 第 4 步：之后完全自动

- 每天 **15:15（北京时间）** 自动抓数据并部署，无需电脑开机；
- 想立刻更新：Actions → daily-update → Run workflow；
- 想改更新时刻：编辑 `.github/workflows/daily.yml` 里的 `cron: '15 7 * * *'`（07:15 UTC = 15:15 北京时间），保存即生效。

## 七、股票池的维护与规则

股票池来源（合并去重）：

1. 红利类型基金近5年（2020至今）前十大持仓（自动剔除“改革红利/港股红利”等非股息基金）；
2. 红利指数成分股：中证红利、红利低波、深证红利、上证红利、红利低波100；
3. 高股息筛选：近12个月已实施分红折算股息率TTM ≥ 3% 且市值 ≥ 100亿；
4. **分红质量过滤**：剔除近3年（含今年）任一年度未实施现金分红的股票；剔除已退市/无行情的股票。

重建股票池（建议每季度做一次）：

- 云端：仓库 → Actions → **build-watchlist** → Run workflow（约5~10分钟），然后再跑一次 daily-update 部署；
- 本地：`python pipeline/build_watchlist.py`，完成后提交 data/meta 下的两个文件并推送。

想调过滤规则（如“近2年每年分红”）：编辑 `config.json` 里的 `dividend_years`。

## 八、股息率阈值提醒（推微信）

### 规则配置

编辑 `data/meta/alerts.json`：

```json
{
  "rules": [
    {"code": "600900", "name": "长江电力", "dy_below": 4.0, "dy_above": 5.5},
    {"code": "601088", "name": "中国神华", "dy_below": 4.5}
  ]
}
```

- `dy_below`：股息率跌破该值时提醒；`dy_above`：涨破时提醒；可只填一个；
- 只有“穿越”那一刻提醒一次，不会每天重复发；
- 改完推送到仓库（或直接在 GitHub 网页上改该文件），下次更新生效。

### 推送渠道（二选一）

**Server酱（推荐，最省事）**
1. 打开 [sct.ftqq.com](https://sct.ftqq.com)，用 GitHub 登录，复制 SendKey（`SCT` 开头）；
2. 仓库 → Settings → Secrets and variables → Actions → New repository secret：名称 `SERVERCHAN_KEY`，值粘贴 SendKey；
3. 用微信扫码关注“方糖”公众号，之后提醒就推到这里。免费版每天有条数限制（日常够用）。

**企业微信群机器人（免费不限条数）**
1. 手机装“企业微信”App，注册免费企业；
2. 建一个群（可只有自己）→ 群设置 → 添加群机器人 → 复制 Webhook；
3. 仓库 Secrets 里新建 `WECOM_WEBHOOK`，粘贴地址。

两个都配了时优先用 Server酱。配置好后手动跑一次 daily-update 即可验证。

## 九、数据与指标口径

| 指标 | 来源与口径 |
|---|---|
| 股息率TTM | 近12个月已实施现金分红合计 ÷ 最新收盘价（东方财富分红数据中心） |
| 市盈率TTM / 市净率 / 总市值 | 东方财富估值数据中心（PE_TTM / PB_MRQ / 总市值） |
| ROE | 最新报告期加权净资产收益率，中报未披露的自动回填一季报 |
| 近1月% / 近3月% | 最新收盘价相对 21 / 63 个交易日前收盘的涨跌幅（由日K线计算） |
| 近5年PB均值 | 近5年（今往前5个自然年，约68个月）各月末 `收盘价 ÷ 当年每股净资产(BPS)` 的算术平均（BPS 取最新年报，按年向前取最近值） |
| 预期收益① | `当前股息率(小数) + (每股收益 − 每股分红金额) × 当前市净率 ÷ 当前股价` |
| 预期收益②(5年PB) | 同上，市净率改用 **近5年PB均值** |
| 国有企业标签 | 实际控制人/控股股东名称命中国资关键词（国资委/国有/国务院/财政部/汇金/证金/社保基金/中国烟草/中央汇金等）标记为“国有企业”；原“基金持仓”标签已移除 |
| K线 | 东方财富前复权日K，自动降级到腾讯源（东财限流时） |
| 板块市值排名 | 按估值数据中的行业板块汇总（涨跌幅来自东财行业板块行情，失败时用成分股加权近似） |
| 排名区块 | 首页三榜：银行股息率 Top10、连续分红≥5年·近1月涨幅/跌幅 Top10（由快照字段实时计算） |
| 公司基本面 | 东方财富 F10：股东研究（控股股东/实控人/十大股东，名称后加（国有）/（民营）括号）、分红融资（股息率TTM/近5年平均股息率/每股分红金额/分红比率）、财务分析（主要财务指标+同比，默认最新年报，可切 年报/一季报/中报/三季报，图表跟随指标行，收录2016至今）、高管增减持（RPTA_WEB_GGMX）、公司概况 |

## 十、常见问题（FAQ）

**1. 网页打不开 / 404？**
确认：① Pages 设置里 Source 已选 GitHub Actions；② 至少手动跑过一次 daily-update。

**2. 数据不更新？**
周末/节假日没有新交易日数据，会保持上一个交易日数据并跳过；工作日 15:15 后手动刷新网页即可。也可以在 Actions 里看 daily-update 是否成功，失败时点进去看红色步骤的日志。

**3. 有些股票指标显示 “—”？**
该股已退市/被吸收合并或长期停牌，属正常。

**3.1 个股页「公司信息」模块显示“数据暂不可用”？**
公司基本面是首次全量抓取 / 每日增量生成的。若某只股票 `data/company/代码.json` 尚未生成（例如刚纳入股票池、或抓取时接口限流失败），该模块会提示暂不可用；等次日 daily-update 跑过、或云端手动跑一次 **build-company** 后即可看到。也可本地 `python pipeline/build_company.py --force` 补齐。

**4. 推送被拒绝（撞车）？**
已内置“推送前自动同步远端”，正常不会再出现；若看到 `rejected (fetch first)`，在本地执行 `git pull --rebase origin main` 后再推。

**5. 密钥会泄漏吗？**
不会。SendKey/Webhook 存放在 GitHub Secrets（加密、日志自动打码），项目文件和本备份中都不包含。

**6. 东财接口限流导致失败？**
程序自带重试与备用源（K线自动切腾讯）；偶尔某次失败不影响历史数据，下次运行会自动补齐。

## 十一、备份与安全

- 本压缩包是完整备份，**不含 K线缓存**（每天自动重新生成，无需备份）和 **.git**（版本历史在 GitHub）；
- 建议每月重新打包一次，或直接以 GitHub 仓库为代码备份；
- 不要把 SendKey、Webhook、任何 `ghp_` 令牌写进项目文件或聊天。

## 十二、免责声明

数据来自公开免费接口（东方财富、腾讯、新浪、天天基金），仅供学习研究参考，不构成任何投资建议；程序可能因第三方接口调整而失效，请以官方数据为准。
