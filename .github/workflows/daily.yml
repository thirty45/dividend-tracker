name: daily-update

on:
  schedule:
    # 每天 16:30 和 21:30（北京时间 = 08:30 / 13:30 UTC）各跑一次：
    # 16:30 保证当天估值与K线已发布；21:30 作为晚间补跑，防止数据延迟漏更新
    - cron: '30 8 * * *'
    - cron: '30 13 * * *'
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: data-update
  cancel-in-progress: false

jobs:
  update-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 240
    steps:
      - name: 检出代码
        uses: actions/checkout@v7

      - name: 安装 Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.12'

      - name: 每日更新数据
        run: python pipeline/update_daily.py
        env:
          PYTHONUNBUFFERED: '1'
          SERVERCHAN_KEY: ${{ secrets.SERVERCHAN_KEY }}
          WECOM_WEBHOOK: ${{ secrets.WECOM_WEBHOOK }}

      - name: 构建红利/宽基基金数据
        run: python pipeline/funds.py
        env:
          PYTHONUNBUFFERED: '1'

      - name: 构建可转债数据
        run: python pipeline/cbonds.py
        env:
          PYTHONUNBUFFERED: '1'

      - name: 构建港股通数据
        run: python pipeline/hk.py
        env:
          PYTHONUNBUFFERED: '1'

      - name: 提交数据变更
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data
          git diff --cached --quiet || git commit -m "chore: 每日数据更新 $(date -u +%F)"
          git pull --rebase -X theirs origin main
          git push

      - name: 配置 Pages
        uses: actions/configure-pages@v6

      - name: 上传 Pages 产物
        uses: actions/upload-pages-artifact@v5
        with:
          path: .

      - name: 部署到 GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
